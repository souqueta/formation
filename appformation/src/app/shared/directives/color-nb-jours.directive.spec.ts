import { ColorNbJoursDirective } from './color-nb-jours.directive';

describe('ColorNbJoursDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNbJoursDirective();
    expect(directive).toBeTruthy();
  });
});
