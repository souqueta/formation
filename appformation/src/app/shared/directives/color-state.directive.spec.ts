import { ColorStateDirective } from './color-state.directive';

describe('ColorStateDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorStateDirective();
    expect(directive).toBeTruthy();
  });
});
